# Delete a theme (soft delete)

Delete a theme (soft delete)

## Usage

``` r
qc_delete_theme(project, id)
```

## Arguments

- project:

  A `qc_project` object.

- id:

  Integer. Theme id.

## Value

Invisibly `NULL`.
