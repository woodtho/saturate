# List all themes

List all themes

## Usage

``` r
qc_list_themes(project)
```

## Arguments

- project:

  A `qc_project` object.

## Value

A tibble: `id`, `name`, `central_concept`, `narrative`, `definition`,
`scope`, `n_categories`, `n_codes`, `status`, `created_at`.
