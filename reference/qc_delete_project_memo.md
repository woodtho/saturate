# Delete a project journal entry (soft delete)

Delete a project journal entry (soft delete)

## Usage

``` r
qc_delete_project_memo(project, id)
```

## Arguments

- project:

  A `qc_project` object.

- id:

  Integer. Memo id.

## Value

Invisibly `NULL`.
