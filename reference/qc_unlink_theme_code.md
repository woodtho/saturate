# Unlink a code from a theme

Unlink a code from a theme

## Usage

``` r
qc_unlink_theme_code(project, theme_id, code_id)
```

## Arguments

- project:

  A `qc_project` object.

- theme_id:

  Integer. Theme id.

- code_id:

  Integer. Code id.

## Value

Invisibly `NULL`.
