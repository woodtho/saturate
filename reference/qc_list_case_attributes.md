# List attributes for one case (long format)

List attributes for one case (long format)

## Usage

``` r
qc_list_case_attributes(project, case_id)
```

## Arguments

- project:

  A `qc_project` object.

- case_id:

  Integer.

## Value

A tibble: `variable`, `value`.
