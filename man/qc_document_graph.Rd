% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{qc_document_graph}
\alias{qc_document_graph}
\title{Build graph data for the document-code network}
\usage{
qc_document_graph(
  project,
  type = c("similarity", "bipartite", "cooccurrence"),
  code_ids = NULL,
  source_ids = NULL,
  min_shared = 1L
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{type}{One of \code{"similarity"} (default), \code{"bipartite"},
\code{"cooccurrence"}.}

\item{code_ids}{Integer vector or \code{NULL}. Restrict to these codes.}

\item{source_ids}{Integer vector or \code{NULL}. Restrict to these documents.}

\item{min_shared}{Integer. For \code{"similarity"}: minimum shared codes for an
edge. For \code{"cooccurrence"}: minimum co-document count.}
}
\value{
A named list: \code{nodes} and \code{edges}, each a tibble formatted for
\code{visNetwork::visNetwork()}.
}
\description{
Returns node and edge tibbles suitable for \code{visNetwork} or \code{igraph}.
Three graph types are supported:
}
\details{
\itemize{
\item \code{"similarity"} -- documents are nodes; edges connect documents that share
at least \code{min_shared} codes, weighted by the number of shared codes.
\item \code{"bipartite"} -- documents \emph{and} codes are nodes; edges represent
individual coding relationships.
\item \code{"cooccurrence"} -- codes are nodes; edges connect codes that co-occur in
at least one document, weighted by the document count.
}
}
