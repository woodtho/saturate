% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{qc_as_igraph}
\alias{qc_as_igraph}
\title{Convert project network data to an igraph object}
\usage{
qc_as_igraph(
  project,
  type = c("cooccurrence", "relations"),
  code_ids = NULL,
  source_ids = NULL,
  min_shared = 1L
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{type}{One of \code{"cooccurrence"} (default) or \code{"relations"}.}

\item{code_ids}{Integer vector or \code{NULL}. Restrict to these codes.}

\item{source_ids}{Integer vector or \code{NULL}. Restrict to these documents
(applies to \code{"cooccurrence"} only).}

\item{min_shared}{Integer. Minimum co-occurrence count to include an edge
(applies to \code{"cooccurrence"} only).}
}
\value{
An igraph object. For \code{"cooccurrence"}, edges carry a \code{weight}
attribute and vertices carry \code{color} and \code{n_codings}. For \code{"relations"},
edges carry a \code{type} attribute (directed graph).
}
\description{
Builds an igraph object from code co-occurrence counts or from explicit
code relations stored in \code{code_relations}.
}
