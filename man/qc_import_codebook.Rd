% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook_io.R
\name{qc_import_codebook}
\alias{qc_import_codebook}
\title{Import a codebook from a file}
\usage{
qc_import_codebook(
  project,
  path,
  format = c("csv", "json"),
  skip_existing = TRUE
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{path}{Character. Path to the import file.}

\item{format}{One of \code{"csv"} (default) or \code{"json"}.}

\item{skip_existing}{Logical. When \code{TRUE} (default), codes whose names
already exist are silently skipped.}
}
\value{
Invisibly, a one-row tibble: \code{imported}, \code{skipped}.
}
\description{
Reads codes from a CSV or JSON file and adds them to the project. Codes
whose names already exist are skipped by default. Category names in the
file are created if absent, then linked to the imported code.
}
\details{
\strong{CSV columns:} \code{name} (required), \code{color}, \code{memo}, \code{definition},
\code{criteria}, \code{code_key}, \code{categories} (comma-separated names in a single
cell).

\strong{JSON format:} array of objects -- \code{name}, \code{color}, \code{memo}, \code{definition},
\code{criteria}, \code{code_key}, \code{categories} (array of strings). JSON import
requires \pkg{jsonlite}.
}
