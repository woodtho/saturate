% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_io.R
\name{qc_split_project}
\alias{qc_split_project}
\title{Create a coder copy of a project}
\usage{
qc_split_project(
  project,
  path,
  source_ids = NULL,
  include_codings = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{path}{Character. Destination file path (\code{.duckdb}).}

\item{source_ids}{Integer vector of source IDs to include, or \code{NULL} for all.}

\item{include_codings}{Logical. Copy existing codings into the split file.}

\item{overwrite}{Logical. Overwrite \code{path} if it already exists.}
}
\value{
The new \code{qc_project} (invisibly). Caller must call \code{\link[=qc_close]{qc_close()}}.
}
\description{
Copies the codebook, cases, themes, and a subset (or all) of documents into
a new standalone \code{.duckdb} file. Codings are excluded by default so each
coder starts fresh. IDs are NOT preserved — the copy gets fresh sequences —
so the files can be merged back later by name / content-hash matching.
}
