% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook_validate.R
\name{qc_validate_codebook}
\alias{qc_validate_codebook}
\title{Validate the codebook for structural and quality issues}
\usage{
qc_validate_codebook(project)
}
\arguments{
\item{project}{A \code{qc_project} object.}
}
\value{
A tibble: \code{code_id}, \code{code_name}, \code{issue_type}, \code{severity}
(\code{"error"}, \code{"warning"}, \code{"info"}), \code{message}. Ordered error -> warning ->
info, then alphabetically by code name. Returns an empty tibble (with a
success message) when no issues are found.
}
\description{
Runs a series of checks and returns a tibble of issues grouped by severity.
Uses a flat SQL fetch (not the recursive CTE) so it is safe to call even
when the hierarchy may be circular.
}
\details{
\strong{Checks performed:}
\itemize{
\item \code{orphan_parent} (error): \code{parent_id} references a non-existent code.
\item \code{circular_hierarchy} (error): parent chain loops back to itself.
\item \code{missing_code_key} (warning): no stable key assigned; call
\code{\link[=qc_set_code_key]{qc_set_code_key()}}.
\item \code{missing_definition} (warning): code has no definition text.
\item \code{missing_criteria} (info): code has no inclusion/exclusion criteria.
\item \code{unused_code} (info): code has zero active codings.
\item \code{deprecated_with_codings} (warning): deprecated code still has active
codings.
}
}
