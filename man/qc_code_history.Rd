% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codes.R
\name{qc_code_history}
\alias{qc_code_history}
\title{Retrieve the change history for one or all codes}
\usage{
qc_code_history(project, code_id = NULL)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{code_id}{Integer or \code{NULL}. When supplied, restricts results to
that code. When \code{NULL}, returns history for all codes.}
}
\value{
A tibble: \code{id}, \code{code_id}, \code{code_name}, \code{operation}, \code{field},
\code{old_value}, \code{new_value}, \code{changed_at}. Ordered newest-first.
}
\description{
Returns an append-only audit log. Each row records one mutation:
a \code{create} event when a code is first added, one \code{update} event per
changed field (with before/after values), and a \code{delete} event when
the code is soft-deleted.
}
