% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documents_import.R
\name{qc_import_batch}
\alias{qc_import_batch}
\title{Import multiple documents from a directory or tabular file}
\usage{
qc_import_batch(
  project,
  path,
  text_col = NULL,
  name_col = NULL,
  metadata_cols = NULL,
  format = NULL,
  language = "",
  file_pattern = "*",
  sheet = 1L,
  skip = 0L
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{path}{Character. Path to a directory, CSV/TSV, or Excel file.}

\item{text_col}{Character. Column name containing document text (tabular
sources only).}

\item{name_col}{Character or \code{NULL}. Column to use as document name
(tabular). When \code{NULL}, names are generated from the source file and
row number.}

\item{metadata_cols}{Character vector or \code{NULL}. Column names whose values
are stored as source attributes.}

\item{format}{One of \code{"dir"}, \code{"csv"}, \code{"tsv"}, \code{"xlsx"}, \code{"xls"}, or
\code{NULL} (auto-detect from extension / path type).}

\item{language}{Character. BCP-47 language tag applied to all imported docs.}

\item{file_pattern}{Glob passed to \code{\link[fs:dir_ls]{fs::dir_ls()}} for directory import
(default \code{"*"}).}

\item{sheet}{Integer. Sheet index for Excel files (default \code{1}).}

\item{skip}{Integer. Rows to skip before the header in tabular files.}
}
\value{
A tibble with one row per imported document: \code{id}, \code{name},
\code{created_at}, \code{row} (source row or filename).
}
\description{
Handles three source types:
\itemize{
\item \strong{Directory}: imports every file whose extension matches \code{file_pattern}.
\item \strong{CSV / TSV}: each row becomes one document; \code{text_col} names the text
column, \code{name_col} the display-name column (optional).
\item \strong{Excel (\code{.xlsx} / \code{.xls})}: same row-per-document model.
}
}
\details{
Columns listed in \code{metadata_cols} are stored as \code{source_attributes} on the
imported document.
}
