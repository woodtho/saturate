% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{qc_code_metrics}
\alias{qc_code_metrics}
\title{Per-code prevalence, density, and Gries' DP dispersion}
\usage{
qc_code_metrics(project, code_ids = NULL, source_ids = NULL)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{code_ids}{Integer vector or \code{NULL}. Restrict to these codes.}

\item{source_ids}{Integer vector or \code{NULL}. Restrict to these documents.}
}
\value{
A tibble: \code{code_id}, \code{code_name}, \code{n_codings}, \code{n_documents},
\code{total_documents}, \code{prevalence}, \code{mean_chars}, \code{total_chars_coded},
\code{density}, \code{dispersion}. Ordered by \code{n_codings} descending.
}
\description{
For each code, computes how broadly it is used (prevalence across
documents), how densely it covers the corpus (density as a percentage of
total characters), and how evenly it is spread (Gries' DP dispersion, where
0 = perfectly uniform, 1 = concentrated in a single document).
}
