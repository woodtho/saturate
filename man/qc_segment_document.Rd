% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documents_import.R
\name{qc_segment_document}
\alias{qc_segment_document}
\title{Segment a document into sub-units}
\usage{
qc_segment_document(
  project,
  source_id,
  method = c("paragraph", "sentence", "speaker_turn", "response_id"),
  min_chars = 20L,
  pattern = NULL,
  keep_parent = TRUE
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{source_id}{Integer. Document to segment.}

\item{method}{Segmentation method: \code{"paragraph"} (split on two or more
consecutive newlines), \code{"sentence"} (linguistic sentence boundaries via
\code{stringi}), \code{"speaker_turn"} (lines beginning with \verb{SPEAKER:} or
\verb{Speaker:}), or \code{"response_id"} (lines beginning with a numeric or
letter-prefixed identifier such as \verb{Q1:} or \code{1.}).}

\item{min_chars}{Integer. Segments shorter than this (after trimming) are
dropped (default \code{20L}).}

\item{pattern}{Character or \code{NULL}. Custom regex overriding the default
turn/ID detection pattern (for \code{"speaker_turn"} and \code{"response_id"}).}

\item{keep_parent}{Logical. When \code{TRUE} (default) the original document is
kept. When \code{FALSE} it is soft-deleted after its segments are created.}
}
\value{
A tibble with one row per segment: \code{id}, \code{name}, \code{created_at},
\code{segment_n}.
}
\description{
Splits the content of an existing document into child documents, each
stored with \code{parent_id} pointing to the original. Useful for splitting
long interview transcripts into paragraphs, sentences, or speaker turns
before coding.
}
