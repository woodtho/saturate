% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{qc_code_matrix}
\alias{qc_code_matrix}
\title{Code-by-entity matrix}
\usage{
qc_code_matrix(
  project,
  by = c("document", "case", "attribute"),
  attribute = NULL,
  code_ids = NULL,
  source_ids = NULL,
  values = c("count", "binary", "chars"),
  coder = NULL
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{by}{One of \code{"document"} (default), \code{"case"}, or \code{"attribute"}.}

\item{attribute}{Character. Required when \code{by = "attribute"}. Name of the
\code{source_attributes.variable} to use as the row dimension.}

\item{code_ids}{Integer vector or \code{NULL}. Restrict columns to these codes.}

\item{source_ids}{Integer vector or \code{NULL}. Restrict to these documents.}

\item{values}{One of \code{"count"} (default), \code{"binary"}, or \code{"chars"}.}

\item{coder}{Character or \code{NULL}. Restrict to codings by this coder.}
}
\value{
A tibble: identifier columns then one column per code, filled with 0
where the code was not applied.
}
\description{
Returns a wide tibble of codes (columns) crossed with entities (rows).
Entities can be documents, cases linked via \code{case_source_links}, or
distinct values of a named source attribute.  Cell values are coding
counts, binary presence/absence flags, or total characters coded.
}
