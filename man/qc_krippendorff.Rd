% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability.R
\name{qc_krippendorff}
\alias{qc_krippendorff}
\title{Krippendorff's alpha for one code across multiple coders}
\usage{
qc_krippendorff(project, code_id, coders = NULL)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{code_id}{Integer. The code to evaluate.}

\item{coders}{Character vector or \code{NULL}. Restrict to these coders.
Defaults to all coders in the project.}
}
\value{
A one-row tibble: \code{code_id}, \code{code_name}, \code{n_coders}, \code{n_units},
\code{observed_disagreement}, \code{expected_disagreement}, \code{alpha}.
}
\description{
Computes Krippendorff's alpha (nominal metric) for a single code across any
number of coders. The unit of analysis is the document: each coder either
applied the code to a document (1) or did not (0). Only documents coded by
at least two of the specified coders contribute to the calculation.
}
\details{
Alpha interpretation: > 0.8 = strong, 0.67–0.8 = tentative, < 0.67 =
unreliable (Krippendorff 2004 thresholds for content analysis).
}
