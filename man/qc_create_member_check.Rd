% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/member_check.R
\name{qc_create_member_check}
\alias{qc_create_member_check}
\title{Create a member check record}
\usage{
qc_create_member_check(
  project,
  source_id,
  participant_label,
  code_ids = NULL,
  created_by = NULL,
  return_by = "",
  return_to = "",
  return_instructions = "",
  notes = ""
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{source_id}{Integer. Document id.}

\item{participant_label}{Character. Identifier for the participant (e.g.
name, pseudonym, or participant ID).}

\item{code_ids}{Integer vector or \code{NULL}. Restrict to specific codes. \code{NULL}
includes all active codings on the document.}

\item{created_by}{Character or \code{NULL}. Records who created this check.
Defaults to the current system user.}

\item{return_by}{Character. Requested return date for the participant's
response (free text, e.g. \code{"2025-03-01"}).}

\item{return_to}{Character. Contact name or address to return feedback to.}

\item{return_instructions}{Character. Instructions for how the participant
should return their response.}

\item{notes}{Character. Internal notes about this member check.}
}
\value{
A one-row tibble: \code{id}, \code{source_id}, \code{participant_label}, \code{status},
\code{sent_at}.
}
\description{
Records that a set of coded segments has been prepared for participant
review. Use \code{\link[=qc_export_member_check]{qc_export_member_check()}} to generate the shareable document
and \code{\link[=qc_record_member_response]{qc_record_member_response()}} to log the participant's feedback.
}
