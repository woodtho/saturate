% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{qc_search_documents}
\alias{qc_search_documents}
\title{Full-text search across documents}
\usage{
qc_search_documents(
  project,
  pattern,
  regex = FALSE,
  ignore_case = TRUE,
  accent_fold = FALSE,
  source_ids = NULL,
  context_chars = 80L
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{pattern}{Character. The search term or regular expression.}

\item{regex}{Logical. When \code{TRUE}, \code{pattern} is treated as a Perl-compatible
regex. When \code{FALSE} (default), it is matched literally.}

\item{ignore_case}{Logical. Case-insensitive search (default \code{TRUE}).}

\item{accent_fold}{Logical. When \code{TRUE} (requires \code{stringi}), both the
document text and \code{pattern} are converted to ASCII-equivalent characters
before matching, enabling accent-insensitive search (e.g. \code{"cafe"} matches
\code{"café"}).}

\item{source_ids}{Integer vector or \code{NULL}. Restrict to these documents.}

\item{context_chars}{Integer. Characters of surrounding text to include on
each side of the match (default 80).}
}
\value{
A tibble: \code{source_id}, \code{source_name}, \code{match_n}, \code{match_start},
\code{match_end}, \code{match_text}, \code{context}.
}
\description{
Finds all occurrences of a pattern within document content, returning each
match with surrounding context.
}
