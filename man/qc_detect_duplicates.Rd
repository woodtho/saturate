% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documents_import.R
\name{qc_detect_duplicates}
\alias{qc_detect_duplicates}
\title{Detect exact and near-duplicate documents}
\usage{
qc_detect_duplicates(
  project,
  threshold = 0.85,
  method = c("both", "exact", "near"),
  source_ids = NULL
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{threshold}{Numeric in \verb{[0, 1]}. Minimum Jaccard similarity to report
as a near-duplicate (default \code{0.85}). Exact duplicates always appear
regardless of threshold.}

\item{method}{One of \code{"both"}, \code{"exact"}, or \code{"near"}.}

\item{source_ids}{Integer vector or \code{NULL}. Restrict comparison to these
documents.}
}
\value{
A tibble: \code{source_id_1}, \code{name_1}, \code{source_id_2}, \code{name_2},
\code{similarity}, \code{type} (\code{"exact"} or \code{"near"}). An empty tibble if no
duplicates are found.
}
\description{
Compares documents pairwise using MD5 hashes (exact duplicates) and
Jaccard similarity on word sets (near-duplicates). No additional packages
are required.
}
