% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{qc_get_coded_segments}
\alias{qc_get_coded_segments}
\title{Retrieve all coded segments, optionally filtered}
\usage{
qc_get_coded_segments(
  project,
  code_ids = NULL,
  must_have = NULL,
  must_not = NULL,
  source_ids = NULL,
  case_ids = NULL,
  category_ids = NULL,
  coder = NULL,
  coding_source = NULL,
  coding_status = NULL,
  source_attrs = NULL,
  limit = NULL,
  offset = 0L
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{code_ids}{Integer vector or \code{NULL}. OR filter -- return segments that
carry any of these codes.}

\item{must_have}{Integer vector or \code{NULL}. AND filter -- restrict to
documents that contain \emph{all} of these codes somewhere (any segment).}

\item{must_not}{Integer vector or \code{NULL}. NOT filter -- exclude documents
that carry any of these codes.}

\item{source_ids}{Integer vector or \code{NULL}. Restrict to these documents.}

\item{case_ids}{Integer vector or \code{NULL}. Restrict to documents linked to
these cases.}

\item{category_ids}{Integer vector or \code{NULL}. Restrict to codes in these
categories.}

\item{coder}{Character or \code{NULL}. Restrict to codings by this coder.}

\item{coding_source}{One of \code{"manual"}, \code{"auto"}, or \code{NULL} for all.}

\item{coding_status}{One of \code{"draft"}, \code{"validated"}, or \code{NULL} for all.}

\item{source_attrs}{Named list of source-attribute filters, e.g.
\code{list(industry = "tech", size = "small")}. Every key-value pair must
match a row in \code{source_attributes} for the document to be included (AND
semantics across pairs).}

\item{limit}{Integer or \code{NULL}. Maximum number of rows to return. \code{NULL}
(default) returns all matching rows. Use with \code{offset} for pagination.}

\item{offset}{Integer. Number of rows to skip before returning results
(default 0). Only meaningful when \code{limit} is set.}
}
\value{
A tibble: \code{coding_id}, \code{source_id}, \code{source_name}, \code{code_id},
\code{code_name}, \code{code_color}, \code{category_names}, \code{selfirst}, \code{selast},
\code{seltext}, \code{memo}, \code{coder}, \code{coding_source}, \code{coding_status},
\code{created_at}.
}
\description{
The primary analysis function. Returns a flat tibble of every coded passage.
}
