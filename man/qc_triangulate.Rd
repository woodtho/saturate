% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangulate.R
\name{qc_triangulate}
\alias{qc_triangulate}
\title{Triangulate codes across source types}
\usage{
qc_triangulate(
  project,
  code_ids = NULL,
  category_ids = NULL,
  metric = c("segments", "documents")
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{code_ids}{Integer vector or \code{NULL}. Restrict to specific codes.}

\item{category_ids}{Integer vector or \code{NULL}. Restrict to codes in these
categories.}

\item{metric}{One of \code{"segments"} (count of coded segments, default) or
\code{"documents"} (count of distinct documents containing the code).}
}
\value{
A wide tibble: one row per code, one column per source type, values
are segment or document counts. Includes a \code{total} column. Rows ordered
by \code{total} descending.
}
\description{
Compares how codes (or themes) are distributed across different
data-collection methods. A code with strong presence across multiple source
types provides better-triangulated evidence than one appearing in only one.
}
\details{
Documents must have a \code{source_type} set via \code{\link[=qc_set_source_type]{qc_set_source_type()}} or the
\code{source_type} argument in \code{\link[=qc_import_document]{qc_import_document()}}. Documents without a type
are grouped under \code{"unspecified"}.
}
