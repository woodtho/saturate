% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saturation.R
\name{qc_saturation_curve}
\alias{qc_saturation_curve}
\title{Compute the code saturation curve}
\usage{
qc_saturation_curve(
  project,
  order_by = c("import_order", "first_coded"),
  code_ids = NULL
)
}
\arguments{
\item{project}{A \code{qc_project} object.}

\item{order_by}{One of \code{"import_order"} (document creation date, default) or
\code{"first_coded"} (earliest coding timestamp in each document).}

\item{code_ids}{Integer vector or \code{NULL}. Restrict to a subset of codes.}
}
\value{
A tibble: \code{doc_index}, \code{doc_name}, \code{source_type}, \code{n_codings},
\code{new_codes}, \code{cumulative_codes}.
}
\description{
Returns one row per document (in import order, or first-coded order) showing
how many new codes were introduced and the running cumulative total. A curve
that flattens signals theoretical saturation — new data is no longer
producing new codes.
}
